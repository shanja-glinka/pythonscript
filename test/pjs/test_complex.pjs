# Тест сложных операторов: циклы, условия, комментарии

def factorial(n):
    # Вычисляет факториал через цикл
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result

x = 5
if x > 0:
    print(factorial(x))
else:
    print("x is not positive")

# Проверка цикла while
y = 0
while y < 5:
    print("y =", y)
    y += 1
